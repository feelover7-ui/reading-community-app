---
description: 뮤즈 스파크 1.3(OpenCode)에 질문하고 답을 받아온다
allowed-tools: Bash(opencode:*)
---

아래는 OpenCode의 뮤즈 스파크 1.3 모델에 `$ARGUMENTS` 를 물어본 결과다.

!`opencode run -m opencode/muse-spark-1.3-contributor-free "$ARGUMENTS"`

이 답변을 사용자에게 정리해서 전달하고, 내 의견이 다르면 그 점도 덧붙여라.
